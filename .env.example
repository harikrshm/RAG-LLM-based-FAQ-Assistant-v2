# Root Environment Variables (if needed for shared configs)
# This file can be used for docker-compose or shared configurations

# Project Configuration
PROJECT_NAME=RAG-LLM-based-FAQ-Assistant
PROJECT_VERSION=1.0.0

# Docker Configuration (if using docker-compose)
# BACKEND_PORT=8000
# FRONTEND_PORT=5173
